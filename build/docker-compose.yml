version: '3' 
services:
  web: 
    build: 
      context: ${DIR_DFILES}
      dockerfile: ./dockerfile
    env_file: 
      - ./.env
    ports: 
      - ${PUERTO}:80
    # volumes: 
    #   - ../app_next:/usr/share/nginx/html
